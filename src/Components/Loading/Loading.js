import React from "react";
import PropTypes from "prop-types";
import { LoadingWrapper } from "./Styles";

const Loading = (props) => {
  return <LoadingWrapper>Test content</LoadingWrapper>;
};

Loading.propTypes = {};

Loading.defaultProps = {};

export default Loading;
